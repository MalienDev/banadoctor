from django.urls import path

app_name = 'search'

urlpatterns = [
    # The doctor search URL has been moved to the 'users' app (users/urls.py)
    # to match the desired URL structure /api/v1/users/search-doctors/.
    # This file is kept for potential future search-related URLs.
]
